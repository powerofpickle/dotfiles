# -- Begin copied from default config
### Variables
#
# Logo key. Use Mod1 for Alt.
# CHANGE FROM DEFAULT
set $mod Mod4

# Lock
set $lock_cmd exec swaylock -i $bg -s fill
bindsym $mod+q+l $lock_cmd

bindsym $mod+q+k exec swaymsg input "type:keyboard" xkb_switch_layout next && notify-send --transient "Switched keyboard layout"

# Toggle notification center
bindsym $mod+q+n exec swaync-client -t -sw

bindsym $mod+q nop  # Needed to not pass down q to focused window

# Apps
bindsym $mod+a+w exec flatpak run io.gitlab.librewolf-community
bindsym $mod+a+v exec pavucontrol  # Pavucontrol
bindsym $mod+a+m exec pavucontrol --tab=4  # Microphone
bindsym $mod+a+c exec flatpak run io.github.ungoogled_software.ungoogled_chromium
bindsym $mod+a+f exec thunar

#bindsym $mod+Control+a exec notify-send "hello"

#set $mod Mod1
# Home row direction keys, like vim
set $left h
set $down j
set $up k
set $right l
# Your preferred terminal emulator
# CHANGE FROM DEFAULT
#set $term foot
set $term alacritty
# Your preferred application launcher
# Note: pass the final command to swaymsg so that the resulting window can be opened
# on the original workspace that the command was run on.
set $menu dmenu_path | dmenu | xargs swaymsg exec --


### Key bindings
#
# Basics:
#
    # Start a terminal
    bindsym $mod+Return exec $term

    # Kill focused window
    bindsym $mod+Shift+q kill

    # Start your launcher
    # CHANGE FROM DEFAULT
    #bindsym $mod+d exec $menu

    # Drag floating windows by holding down $mod and left mouse button.
    # Resize them with right mouse button + $mod.
    # Despite the name, also works for non-floating windows.
    # Change normal to inverse to use left mouse button for resizing and right
    # mouse button for dragging.
    floating_modifier $mod normal

    # Reload the configuration file
    bindsym $mod+Shift+c reload

    # Exit sway (logs you out of your Wayland session)
    bindsym $mod+Shift+e exec swaynag -t warning -m 'You pressed the exit shortcut. Do you really want to exit sway? This will end your Wayland session.' -B 'Yes, exit sway' 'swaymsg exit'
#
# Moving around:
#
    # Move your focus around
    #bindsym $mod+$left focus left
    #bindsym $mod+$down focus down
    #bindsym $mod+$up focus up
    #bindsym $mod+$right focus right
    # Or use $mod+[up|down|left|right]
    bindsym $mod+Left focus left
    bindsym $mod+Down focus down
    bindsym $mod+Up focus up
    bindsym $mod+Right focus right

    # Move the focused window with the same, but add Shift
    bindsym $mod+Shift+$left move left
    bindsym $mod+Shift+$down move down
    bindsym $mod+Shift+$up move up
    bindsym $mod+Shift+$right move right
    # Ditto, with arrow keys
    bindsym $mod+Shift+Left move left
    bindsym $mod+Shift+Down move down
    bindsym $mod+Shift+Up move up
    bindsym $mod+Shift+Right move right
#
# Workspaces:
#
    # Switch to workspace
    bindsym $mod+1 workspace number 1
    bindsym $mod+2 workspace number 2
    bindsym $mod+3 workspace number 3
    bindsym $mod+4 workspace number 4
    bindsym $mod+5 workspace number 5
    bindsym $mod+6 workspace number 6
    bindsym $mod+7 workspace number 7
    bindsym $mod+8 workspace number 8
    bindsym $mod+9 workspace number 9
    bindsym $mod+0 workspace number 10
    # Move focused container to workspace
    bindsym $mod+Shift+1 move container to workspace number 1
    bindsym $mod+Shift+2 move container to workspace number 2
    bindsym $mod+Shift+3 move container to workspace number 3
    bindsym $mod+Shift+4 move container to workspace number 4
    bindsym $mod+Shift+5 move container to workspace number 5
    bindsym $mod+Shift+6 move container to workspace number 6
    bindsym $mod+Shift+7 move container to workspace number 7
    bindsym $mod+Shift+8 move container to workspace number 8
    bindsym $mod+Shift+9 move container to workspace number 9
    bindsym $mod+Shift+0 move container to workspace number 10
    # Note: workspaces can have any name you want, not just numbers.
    # We just use 1-10 as the default.
#
# Layout stuff:
#
    # You can "split" the current object of your focus with
    # $mod+b or $mod+v, for horizontal and vertical splits
    # respectively.
    bindsym $mod+b splith
    bindsym $mod+v splitv

    # Switch the current container between different layout styles
    bindsym $mod+s layout stacking
    bindsym $mod+w layout tabbed
    bindsym $mod+e layout toggle split

    # Make the current focus fullscreen
    bindsym $mod+f fullscreen

    # Toggle the current focus between tiling and floating mode
    bindsym $mod+Shift+space floating toggle

    # Swap focus between the tiling area and the floating area
    bindsym $mod+space focus mode_toggle

    # Move focus to the parent container
    bindsym $mod+a focus parent
#
# Scratchpad:
#
    # Sway has a "scratchpad", which is a bag of holding for windows.
    # You can send windows there and get them back later.

    # Move the currently focused window to the scratchpad
    bindsym $mod+Shift+minus move scratchpad

    # Show the next scratchpad window or hide the focused scratchpad window.
    # If there are multiple scratchpad windows, this command cycles through them.
    bindsym $mod+minus scratchpad show
#
# Resizing containers:
#
mode "resize" {
    # left will shrink the containers width
    # right will grow the containers width
    # up will shrink the containers height
    # down will grow the containers height
    bindsym $left resize shrink width 10px
    bindsym $down resize grow height 10px
    bindsym $up resize shrink height 10px
    bindsym $right resize grow width 10px

    # Ditto, with arrow keys
    bindsym Left resize shrink width 10px
    bindsym Down resize grow height 10px
    bindsym Up resize shrink height 10px
    bindsym Right resize grow width 10px

    # Return to default mode
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+r mode "resize"
# -- End copied from default config


set $menu fuzzel
bindsym $mod+d exec $menu

set $dmenu dmenu_path | fuzzel --dmenu | xargs swaymsg exec --
bindsym $mod+Shift+d exec $dmenu


# Pavucontrol
bindsym $mod+Shift+v exec pavucontrol
# Mic control

# pactl
bindsym XF86AudioRaiseVolume exec pactl set-sink-volume @DEFAULT_SINK@ +5%
bindsym XF86AudioLowerVolume exec pactl set-sink-volume @DEFAULT_SINK@ -5%
bindsym Shift+XF86AudioRaiseVolume exec pactl set-sink-volume @DEFAULT_SINK@ +1%
bindsym Shift+XF86AudioLowerVolume exec pactl set-sink-volume @DEFAULT_SINK@ -1%
bindsym XF86AudioMute exec pactl set-sink-mute @DEFAULT_SINK@ toggle

bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute 1 toggle

bindsym XF86MonBrightnessUp exec brightnessctl set +5%
bindsym XF86MonBrightnessDown exec brightnessctl set 5%-
bindsym Shift+XF86MonBrightnessUp exec brightnessctl set +1%
bindsym Shift+XF86MonBrightnessDown exec brightnessctl set 1%-

# MPRIS controls (headphone controls)
bindsym XF86AudioPlay exec playerctl play-pause
bindsym XF86AudioPause exec playerctl play-pause
bindsym XF86AudioNext exec playerctl next
bindsym XF86AudioPrev exec playerctl previous

bindsym $mod+m exec playerctl play-pause

# Workspace navigation
bindsym $mod+u workspace prev
bindsym $mod+p workspace next
bindsym $mod+Shift+u move workspace to output left
bindsym $mod+Shift+p move workspace to output right

bindsym $mod+Control+e workspace prev  # Left side of keyboard
bindsym $mod+Control+d workspace next

# Tab navigation
bindsym $mod+i exec sway-overfocus group-lw group-uw
bindsym $mod+o exec sway-overfocus group-rw group-dw

bindsym $mod+Control+q exec sway-overfocus group-lw group-uw  # Left side of keyboard
bindsym $mod+Control+w exec sway-overfocus group-rw group-dw

# Window navigation
bindsym $mod+h exec sway-overfocus split-lt float-lt output-ls
bindsym $mod+j exec sway-overfocus split-dt float-dt output-ds
bindsym $mod+k exec sway-overfocus split-ut float-ut output-us
bindsym $mod+l exec sway-overfocus split-rt float-rt output-rs

bindsym $mod+Control+a exec sway-overfocus split-lt float-lt output-ls  # Left  # Left side of keyboard
#bindsym $mod+Control+s exec sway-overfocus split-dt float-dt output-ds  # Down
#bindsym $mod+Control+w exec sway-overfocus split-ut float-ut output-us  # Up
bindsym $mod+Control+s exec sway-overfocus split-rt float-rt output-rs  # Right

#bindsym $mod+Control+q workspace prev
#bindsym $mod+Control+w workspace next
#bindsym $mod+Control+a exec sway-overfocus group-lw group-uw
#bindsym $mod+Control+s exec sway-overfocus group-rw group-dw
#bindsym $mod+Control+z exec sway-overfocus split-lt float-lt output-ls
#bindsym $mod+Control+x exec sway-overfocus split-rt float-rt output-rs

# Open terminal with cwd
set $term_open_dir $term --working-directory
bindsym $mod+Shift+Return exec $term_open_dir "$(focused-cwd)"

# Screenshots
bindsym Print exec grim -g "$(slurp -d)" ~/Pictures/Screenshots/$(date +'%Y-%m-%d-%H%M%S_grim.png')
bindsym $mod+Print exec grim ~/Pictures/Screenshots/$(date +'%Y-%m-%d-%H%M%S_grim.png')

# Type emoji
bindsym $mod+period exec BEMOJI_PICKER_CMD="fuzzel --dmenu" bemoji -t -p

# Kill window
#bindsym $mod+q kill

# Mod1 is Alt
#bindsym Mod1+1 workspace number 1
#bindsym Mod1+2 workspace number 2
#bindsym Mod1+3 workspace number 3
#bindsym Mod1+4 workspace number 4
#bindsym Mod1+5 workspace number 5
#bindsym Mod1+6 workspace number 6
#bindsym Mod1+7 workspace number 7
#bindsym Mod1+8 workspace number 8
#bindsym Mod1+9 workspace number 9
#bindsym Mod1+0 workspace number 10

#bindsym Control+$mod+m workspace number 1
#bindsym Control+$mod+comma workspace number 2
#bindsym Control+$mod+period workspace number 3
#bindsym Control+$mod+j workspace number 4
#bindsym Control+$mod+k workspace number 5
#bindsym Control+$mod+l workspace number 6
#bindsym Control+$mod+u workspace number 7
#bindsym Control+$mod+i workspace number 8
#bindsym Control+$mod+o workspace number 9
#bindsym Control+$mod+space workspace number 10

#bindsym Super_R+Shift+q workspace number 0
#bindsym Super_R+Shift+w workspace number 1
#bindsym Super_R+Shift+e workspace number 2
#bindsym Super_R+Shift+a workspace number 3
#bindsym Super_R+Shift+s workspace number 4
#bindsym Super_R+Shift+d workspace number 5
#bindsym Super_R+Shift+z workspace number 6
#bindsym Super_R+Shift+x workspace number 7
#bindsym Super_R+Shift+c workspace number 8

#bindsym $mod+Tab exec sway-overfocus group-rw group-dw
#bindsym $mod+Shift+Tab exec sway-overfocus group-lw group-uw


#bindsym $mod+$left focus left
#bindsym $mod+$down focus down
#bindsym $mod+$up focus up
#bindsym $mod+$right focus right

bindsym $mod+c focus child

#bindsym $mod+Space exec notify-send "Win+Space pressed"

# Clear clipboard
bindsym Control+Shift+b exec wl-copy taco && notify-send --transient "Replaced clipboard with $(wl-paste)"
